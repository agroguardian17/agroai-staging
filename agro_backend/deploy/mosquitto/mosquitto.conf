# Mosquitto 2.x configuration for AgroGuardian.
# Two listeners:
#   1883 - plain MQTT, bound to localhost only (dev compose maps 127.0.0.1 -> 1883)
#   8883 - MQTT over TLS, internet-facing (Caddy terminates TLS in production)
#
# Per-device username/password lives in /mosquitto/config/passwd (bcrypt-hashed).
# Per-device ACL lives in /mosquitto/config/acl. Both are bind-mounted by
# docker-compose.prod.yml; in dev we allow anonymous on 1883 only.

# ----- Persistence + logging --------------------------------------------
persistence true
persistence_location /mosquitto/data/
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
log_timestamp true
log_timestamp_format %Y-%m-%dT%H:%M:%S

# ----- Listener 1: plain MQTT (dev only; bound to internal docker network)
per_listener_settings true
listener 1883 0.0.0.0
protocol mqtt
allow_anonymous true

# ----- Listener 2: MQTT over TLS ----------------------------------------
# Caddy reverse-proxies 8883 -> internal mosquitto :8883 with TLS termination
# so the broker sees clear traffic but the wire is TLS to the device.
listener 8883 0.0.0.0
protocol mqtt
allow_anonymous false
password_file /mosquitto/config/passwd
acl_file /mosquitto/config/acl

# Connection limits (room for a fleet of 50+ Main Nodes).
max_connections 1000
max_inflight_messages 100
max_queued_messages 1000
queue_qos0_messages true

# QoS 1 retained-message expiration (avoid stale telemetry surviving forever).
persistent_client_expiration 7d
